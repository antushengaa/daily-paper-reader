---
title: Low-Complexity Gridless Single-Snapshot DoA Estimation via Truncated Hankel Newton-MUSIC
title_zh: 基于截断Hankel Newton-MUSIC的低复杂度无网格单快拍DoA估计
authors: "Ruoxiao Cao, Wentao Yu, Yi Gong, Khaled B. Letaief"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08108v1"
tags: ["query:gso"]
score: 8.0
evidence: 无网格单快照DoA估计用于阵列处理
tldr: 针对可重构天线阵列ISAC系统中单快拍DoA估计面临子空间秩亏和计算成本高的问题，提出截断Hankel牛顿MUSIC方法。通过固定行维构造截断Hankel矩阵恢复有效信号子空间，避免全矩阵特征分解，将复杂度降为线性于天线端口数；再利用牛顿更新消除网格量化误差。仿真表明，该方法在保持接近平方Hankel牛顿MUSIC精度的同时，运行时间降低两个数量级，适用于实时传感。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08108v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 719, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08108v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 718, \"height\": 576, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08108v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1621, \"height\": 321, \"label\": \"Table\"}]"
motivation: 单快拍场景下子空间方法秩亏，传统Hankel空间平滑计算复杂高，无法满足实时ISAC需求。
method: 构造固定行维的截断Hankel矩阵恢复子空间，避免平方复杂度，并用牛顿迭代在连续域精化粗网格估计。
result: 精度接近平方Hankel牛顿MUSIC，大规模阵列运行时间降低两个数量级以上。
conclusion: 提出的低复杂度方法实现了单快拍高精度DoA估计，适合可重构天线ISAC实时系统。
---

## 摘要
可重构天线阵列能够为集成感知与通信（ISAC）系统提供增强的空间自由度（DoFs），从而实现高分辨率的到达方向（DoA）估计。然而，在高动态场景中，DoA估计必须在较短的相干间隔内完成，这通常将处理限制为单快拍。传统的子空间方法因此面临秩亏问题，而基于Hankel的空间平滑在阵列规模较大时计算成本高昂。本文提出了一种低复杂度的无网格截断Hankel Newton-MUSIC框架，用于单快拍DoA估计。所提方法构建了一个具有固定行维度的截断Hankel矩阵，以恢复有效信号子空间，同时降低相关构建和子空间分解的成本。当截断长度与阵列规模无关时，主要复杂度与天线端口数量呈线性关系。为了减少网格引起的量化误差，通过连续角度域上的二阶牛顿更新进一步细化粗网格估计。仿真结果表明，所提方法在实现接近方形Hankel Newton-MUSIC的DoA估计精度的同时，大幅减少了运行时间。对于大型阵列，与传统的方形Hankel MUSIC相比，它提供了两个数量级以上的运行时间减少，使其适用于可重构天线支持的ISAC系统中的实时感知。

## Abstract
Reconfigurable antenna arrays can provide enhanced spatial Degrees of Freedom (DoFs) for Integrated Sensing And Communication (ISAC) systems, enabling high-resolution Direction of Arrival (DoA) estimation. In highly dynamic scenarios, however, DoA estimation must be performed within short coherence intervals, which often restricts processing to a single snapshot. Conventional subspace methods then suffer from rank deficiency, while Hankel-based spatial smoothing incurs high computational cost when the array size is large. This paper proposes a low-complexity gridless Truncated Hankel NewtonMUSIC framework for single-snapshot DoA estimation. The proposed method constructs a truncated Hankel matrix with a fixed row dimension to recover an effective signal subspace while reducing the cost of correlation construction and subspace decomposition. When the truncation length is independent of the array size, the dominant complexity scales linearly with the number of antenna ports. To reduce grid-induced quantization errors, coarse grid estimates are further refined by a secondorder Newton update in the continuous angular domain. Simulation results show that the proposed method achieves DoA estimation accuracy close to square Hankel Newton-MUSIC while substantially reducing runtime. For large arrays, it provides more than two orders of magnitude runtime reduction compared with conventional square Hankel MUSIC, making it suitable for realtime sensing in reconfigurable antenna-enabled ISAC systems.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：在集成感知与通信（ISAC）系统中，可重构天线阵列能提供更多空间自由度，提升角度分辨率。然而，在高动态移动场景下，信道相干间隔极短，DoA估计仅限于单次采样（单快拍）。传统子空间方法（如MUSIC）依赖多快拍构建满秩协方差矩阵，单快拍下秩亏至1，无法区分多个信号源。Hankel空间平滑可恢复秩，但传统方法采用近方形Hankel矩阵（窗长约为阵元数一半），相关构建和子空间分解的计算复杂度为O(N³)，在大规模阵列下难以满足实时感知需求。
- **整体含义**：本文提出一种低复杂度无网格单快拍DoA估计框架，通过截断Hankel矩阵固定行维，将复杂性降为线性，并利用牛顿迭代消除网格量化误差，面向可重构天线ISAC系统实现实时高精度测角。

### 2. 方法学：核心思想、关键技术细节与算法流程
- **核心思想**：利用源数远小于阵元数的稀疏性（K ≪ N），将Hankel矩阵的行维截断至很小的固定值LN（满足LN+1 > K且LN ≪ N），从而降低平滑相关矩阵的维度。截断后保留大量列（子阵列快拍），仍能恢复有效信号子空间。然后用粗网格初值，再通过二阶牛顿更新在连续角度域精化估计，避免密集网格搜索。
- **关键技术细节**：
  - 构建截断Hankel矩阵HL∈C^(LN+1)×(N-LN)，其中第i行为y[i:i+N-LN-1]。
  - 计算平滑相关矩阵RL = (1/(N-LN)) HL HL^H，尺寸为(LN+1)×(LN+1)。
  - 对RL进行SVD，得噪声子空间Un（由最小的(LN+1-K)个奇异向量构成）。
  - 定义MUSIC代价函数J(θ)=aL^H(θ)UnUn^H aL(θ)，其中aL(θ)为截断子阵的导向矢量（长度LN+1）。
  - 使用粗网格Θ_coarse（如0.5°间隔）找J(θ)的K个局部最小值作为初始估计。
  - 对每个初始值进行Z次牛顿迭代（Z=20）：θ^(z) = θ^(z-1) - ∇J / ∇²J，其中梯度∇J和Hessian∇²J有解析表达式（见公式(14)-(17)），涉及导向矢量的一阶、二阶偏导。K个源的迭代可并行。
- **算法流程**（详见Algorithm 1）：
    1. 输入：单个快照y，源数K，粗网格Θ_coarse，最大迭代Z，截断窗长LN。
    2. 构造截断Hankel矩阵HL。
    3. 计算RL并SVD得Un。
    4. 在Θ_coarse上评估J(θ)，选取K个最小点作为初值θ^(0)。
    5. 对每个源并行执行Z次牛顿更新。
    6. 输出连续角度估计。

### 3. 实验设计
- **场景设置**：单快拍，均匀线阵（N=256），K=4个远场窄带源，DoA在[-60°,60°]均匀随机且最小间隔9°，路径增益随机（动态范围≤10 dB）。蒙特卡罗试验次数M=2000。
- **基准方法**：
  - 方形Hankel MUSIC：窗长LO=128，密集网格搜索（间隔0.1°）。
  - 截断Hankel MUSIC：LN=20，密集网格搜索。
  - 方形Hankel Newton-MUSIC：窗长LO=128，粗网格（0.5°）+牛顿迭代。
  - 截断Hankel Newton-MUSIC（本文方法）：LN=20，粗网格+牛顿迭代。
- **评价指标**：RMSE（根均方误差，考虑排列对齐）和平均运行时间（秒/快照）。
- **对比条件**：SNR从-5到30 dB变化；阵元数从64到1024变化。

### 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量或训练时长。计算复杂度分析以理论flops和仿真运行时长为依据，所有实验均在标准计算平台（推测为CPU或通用服务器）上完成。文中仅报告了“平均运行时间”作为衡量指标。

### 5. 实验数量与充分性
- **实验数量**：两组主要实验：①RMSE随SNR变化（图1），②运行时间随阵元数变化（图2）。每个数据点2000次蒙特卡洛，统计稳定性较好。
- **充分性**：覆盖了低SNR至高SNR，以及不同阵列规模，对比了四种方法的关键变体，能清晰体现截断和牛顿更新的各自贡献。但缺少对更复杂场景（如相干源、近场源、非均匀阵列）的评估，也未做消融实验改变截断长度LN或网格密度。整体而言，实验设计聚焦于所提方法的两个核心优势（低复杂度、无网格精度），具有一定充分性但广度有限。
- **公平性**：对比方法均采用相同的信号模型和评价标准，牛顿迭代的初始网格均使用0.5°粗网格，对比合理。

### 6. 主要结论与发现
- **精度**：在高SNR（>10 dB）下，有网格搜索的方法（方形/截断Hankel MUSIC）因网格量化误差出现RMSE底噪约-33.6 dBrad；而使用牛顿迭代的方法（方形/截断Hankel Newton-MUSIC）持续下降，无误差底噪。截断Hankel Newton-MUSIC与方形Hankel Newton-MUSIC精度差距仅约1.5 dB，表明截断造成的分辨率损失很小并可被牛顿更新补偿。
- **运行时间**：当阵元数从64增至1024，方形Hankel方法运行时间呈立方增长，截断Hankel方法（LN=20）接近线性增长。在N=1024时，本文方法比传统方形Hankel MUSIC快两个数量级以上（超过100倍）。
- **结论**：所提框架在大型阵列下实现了接近最优的精度，同时大幅降低计算负担，适用于可重构天线ISAC系统的实时DoA估计。

### 7. 优点
- **方法设计巧妙**：利用源数稀疏性，通过截断行维将SVD复杂度从O(N³)降为O(L_N³) + O(L_N²N)，其中L_N固定，整体线性于N。
- **无网格能力**：牛顿更新避免密集网格搜索，消除量化误差，且每个源可独立并行，进一步降低延迟。
- **通用性强**：基于Hankel平滑，适用于任意单快拍阵列信号处理（不限于ISAC），且对信噪比适用性好。
- **实验验证充分**：Monte Carlo次数多，对比了有无截断和有无牛顿更新的四种组合，清晰解耦两个技术创新点。

### 8. 不足与局限
- **实验覆盖有限**：仅仿真了均匀线阵和独立同分布噪声场景，未考虑阵列校准误差、互耦、相干源（Hankel平滑本身可解相关，但未验证）、近场模型、或不同天线配置（如可重构天线其他非ULA拓扑）。
- **截断长度选择**：未讨论LN如何自适应选择（文中取20，需已知K），若K未知或时变，需先估计源数，增加了预处理步骤。
- **牛顿更新收敛性**：初始粗网格可能落入错误局部极小值，且仅假设最多Z=20次迭代，未分析不同初始间距下的收敛保证。
- **未提及硬件资源**：论文未报告实际运行代码的平台（如CPU型号、内存），无法直接复现运行时长的绝对值。
- **实际应用限制**：假设已知源数K，实际中需配合源数估计；且可重构天线阵列初始化成ULA的假设简化了硬件复杂度，但未考虑天线端口切换耗时。

（完）
